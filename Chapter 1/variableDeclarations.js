let counter = 0; // Initializing counter.
counter = "zero"; //Updating counter to a string.

// Initialize a variable with a const statement if it will never change.
const PI = 3.141592653589793;

/*
    If you try to modify a value contained in a const, a run-time error occurs.
    PI = 3.14
    Will result in a run-time error.
*/

// You can declare multipl variables with a single const or let statement.
const FREEZING = 0,
	BOILING = 100;
let x, y;
